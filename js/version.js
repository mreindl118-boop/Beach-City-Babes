// Single source of truth for the app version (mirrored in version.json + sw.js
// by tools/bump_version.py — run it for every release).
export const VERSION = '0.9.1-alpha';
export const BUILD = 12;
